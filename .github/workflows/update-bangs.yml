name: Update Bang List

on:
  schedule:
    - cron: '*/5 * * * *'     # every 5 minutes (GitHub’s minimum)
    
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🕒 Scheduled!"
