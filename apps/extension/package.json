{
  "name": "gulgle-extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm --filter gulgle-web build:extension && node ./scripts/build.mjs",
    "typecheck": "node --check ./scripts/build.mjs"
  }
}
